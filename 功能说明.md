# 学生在线答题系统功能说明

## 1. 文档概述

本文档基于 `\student-answer-system` 项目源码编写，用于说明学生在线答题系统的功能范围、角色权限、业务流程、接口能力、数据表结构和运行配置。

系统面向管理员、教师、学生三类用户，提供账号与学生档案维护、试卷题库管理、在线答题、自动判分、人工批改、成绩导出、考前提醒、资料文件共享、讨论交流和个人资料维护等功能。

## 2. 项目概况

### 2.1 系统名称

学生在线答题系统。

### 2.2 系统定位

本系统是一个在线考试与答题管理平台，支持教师或管理员发布试卷，学生在规定时间内在线答题，系统自动保存答题记录并完成客观题判分；教师或管理员可查看成绩、人工调整题目得分并导出成绩表。

### 2.3 主要用户

| 角色 | 说明 |
| --- | --- |
| 管理员 `ADMIN` | 负责系统账号、学生档案、试卷题库、成绩、文件和讨论等全量管理。 |
| 教师 `TEACHER` | 负责试卷题库维护、学生信息查看、成绩查看与批改、资料上传和讨论交流。 |
| 学生 `STUDENT` | 负责查看可答试卷、在线答题、查看个人成绩、接收考试提醒、下载资料和参与讨论。 |

## 3. 技术架构

### 3.1 后端技术

| 技术 | 用途 |
| --- | --- |
| Spring Boot 3.3.5 | 后端应用基础框架。 |
| Spring Web | REST API 接口。 |
| Spring Data JPA | 数据持久化。 |
| Spring Security | 登录认证、角色授权、接口访问控制。 |
| Spring Data Redis | 登录 token 存储。 |
| MySQL 8 | 业务数据存储。 |
| BCrypt | 密码哈希存储。 |
| Apache POI | 成绩 Excel 导出。 |
| Jakarta Validation | 请求参数校验。 |

### 3.2 前端技术

| 技术 | 用途 |
| --- | --- |
| Vue 3 | 前端页面和组件开发。 |
| Vue Router 4 | 页面路由和前端权限跳转。 |
| Vite 6 | 前端开发构建工具。 |
| Axios | HTTP 请求封装。 |
| lucide-vue-next | 页面图标。 |

### 3.3 项目目录

```text
student-answer-system
├── backend/      Spring Boot 后端源码与构建产物
├── frontend/     Vue 3 + Vite 前端源码
├── database/     MySQL 初始化脚本
├── uploads/      上传文件保存目录
└── README.md     项目基础说明
```

## 4. 运行环境与基础配置

### 4.1 环境要求

| 环境 | 要求 |
| --- | --- |
| JDK | 17 |
| Maven | 3.9+ |
| Node.js | 18+ |
| MySQL | 8 |
| Redis | 本地或可连接的 Redis 服务 |

### 4.2 默认服务配置

| 配置项 | 默认值 |
| --- | --- |
| 后端端口 | `8080` |
| 前端端口 | `5173` |
| MySQL 地址 | `localhost:3306` |
| 数据库名 | `student_answer_system` |
| 数据库用户 | `root` |
| 数据库密码 | `123456` |
| Redis 地址 | `localhost:6379` |
| Redis database | `0` |
| 登录 token 有效期 | 12 小时 |
| 考前提醒提前天数 | 3 天 |
| 上传文件目录 | `E:/student-answer-system/uploads` |
| 单文件上传大小上限 | 50MB |
| 单请求上传大小上限 | 50MB |

### 4.3 配置文件位置

```text
backend/src/main/resources/application.yml
frontend/vite.config.js
```

前端 Vite 已配置 `/api` 代理到 `http://localhost:8080`。

## 5. 初始化与默认账号

### 5.1 数据库初始化

执行 `database/init.sql` 可创建数据库、业务表和示例数据。

```powershell
$OutputEncoding = [System.Text.UTF8Encoding]::new($false)
Get-Content -Raw -Encoding UTF8 E:\student-answer-system\database\init.sql | mysql --default-character-set=utf8mb4 -uroot -p123456
```

### 5.2 默认账号

初始化脚本内置三个示例账号，密码均为 `123456`，数据库保存的是 BCrypt 哈希值。

| 角色 | 用户名 | 密码 | 姓名 |
| --- | --- | --- | --- |
| 管理员 | `admin` | `123456` | 系统管理员 |
| 教师 | `teacher` | `123456` | 李老师 |
| 学生 | `student` | `123456` | 王同学 |

示例学生学号为 `2052443662`。

## 6. 启动方式

### 6.1 启动后端

```powershell
cd .\student-answer-system\backend
mvn spring-boot:run
```

后端访问地址：

```text
http://localhost:8080
```

### 6.2 启动前端

```powershell
cd .\student-answer-system\frontend
npm install
npm run dev
```

前端访问地址：

```text
http://localhost:5173
```

## 7. 角色权限说明

### 7.1 前端菜单权限

| 功能菜单 | 管理员 | 教师 | 学生 |
| --- | --- | --- | --- |
| 工作台 | 支持 | 支持 | 支持 |
| 学生管理 | 支持 | 只读查看 | 不支持 |
| 账号管理 | 支持 | 不支持 | 不支持 |
| 试卷管理 | 支持 | 支持 | 不支持 |
| 在线答题 | 不支持 | 不支持 | 支持 |
| 成绩记录 | 支持 | 支持 | 支持个人成绩 |
| 讨论区 | 支持 | 支持 | 支持 |
| 资料文件 | 支持 | 支持 | 支持 |
| 考试提醒 | 不支持 | 不支持 | 支持 |
| 个人中心 | 支持 | 支持 | 支持 |

### 7.2 后端接口权限

| 权限类型 | 说明 |
| --- | --- |
| 公开接口 | 登录、教师注册、文件下载接口可不登录访问。 |
| 登录接口 | 除公开接口外，其余接口都需要携带有效 token。 |
| 管理员接口 | 账号管理、学生新增/编辑/删除等接口仅管理员可用。 |
| 管理员/教师接口 | 试卷题库维护、人工批改、成绩导出、手动生成提醒等接口由管理员或教师使用。 |
| 学生接口 | 在线提交答案、查看本人提醒、标记提醒已读等接口仅学生使用。 |

## 8. 前端页面与路由

| 路由 | 页面 | 访问角色 | 功能说明 |
| --- | --- | --- | --- |
| `/login` | 登录 | 公开 | 输入用户名和密码登录系统。 |
| `/register` | 教师注册 | 公开 | 公开注册教师账号，注册后自动登录。 |
| `/dashboard` | 工作台 | 管理员、教师、学生 | 展示统计数据、最近试卷、最近成绩和学生提醒。 |
| `/students` | 学生管理 | 管理员、教师 | 查看学生列表；管理员可新增、编辑、删除学生。 |
| `/users` | 账号管理 | 管理员 | 管理管理员和教师账号。 |
| `/papers` | 试卷管理 | 管理员、教师 | 维护试卷和题目。 |
| `/exams` | 在线答题列表 | 学生 | 查看已发布试卷和答题状态。 |
| `/exams/:id` | 答题页面 | 学生 | 按题型填写答案并提交。 |
| `/submissions` | 成绩记录 | 管理员、教师、学生 | 查看成绩列表、详情；管理员/教师可改分和导出。 |
| `/discussions` | 讨论区 | 管理员、教师、学生 | 发起讨论、回复、搜索、关联试卷。 |
| `/files` | 资料文件 | 管理员、教师、学生 | 上传、下载、删除资料文件。 |
| `/reminders` | 考试提醒 | 学生 | 查看个人考试提醒并标记已读。 |
| `/profile` | 个人中心 | 管理员、教师、学生 | 修改姓名、头像和密码，学生可查看档案信息。 |

未登录访问受保护页面时，前端会跳转到 `/login`；已登录用户访问公开登录/注册页时会跳转到 `/dashboard`；角色不匹配的路由会跳转回 `/dashboard`。

## 9. 功能模块说明

### 9.1 登录与注册

#### 9.1.1 登录

用户输入用户名和密码后，系统执行以下逻辑：

1. 根据用户名查询账号。
2. 校验账号是否启用。
3. 使用 BCrypt 校验密码。
4. 登录成功后生成随机 token。
5. 将当前用户信息写入 Redis，key 前缀为 `auth:token:`。
6. 前端将 token 和用户信息保存到 `localStorage`。
7. 后续请求自动在请求头中携带 `Authorization: Bearer <token>`。

登录成功返回的用户信息包括用户 ID、用户名、姓名、角色、学生档案 ID 和头像地址。

#### 9.1.2 退出登录

用户点击退出后，后端根据请求头中的 token 删除 Redis 登录态，前端清除本地 token 和用户信息并返回登录页。

#### 9.1.3 教师注册

公开注册仅支持教师账号。注册成功后系统自动创建登录态并进入教师端。

业务规则：

| 规则 | 说明 |
| --- | --- |
| 用户名唯一 | 已存在的用户名不能再次注册。 |
| 公开注册角色限制 | 即使请求中传入其他角色，也只允许注册教师账号。 |
| 学生账号限制 | 学生账号不能公开注册，只能由管理员在学生管理中添加。 |
| 密码存储 | 注册密码使用 BCrypt 哈希后保存。 |

### 9.2 工作台

工作台根据角色展示不同数据。

#### 管理员工作台

| 展示项 | 说明 |
| --- | --- |
| 学生数 | 当前学生档案总数。 |
| 账号数 | 当前管理员和教师等账号总数。 |
| 试卷数 | 当前试卷总数。 |
| 成绩记录 | 当前提交记录总数。 |
| 最近试卷 | 展示最近试卷的标题、开始时间、结束时间、总分和发布状态。 |
| 最近成绩 | 展示最近提交的成绩记录。 |

#### 教师工作台

| 展示项 | 说明 |
| --- | --- |
| 学生数 | 当前学生档案总数。 |
| 试卷数 | 当前试卷总数。 |
| 成绩记录 | 当前提交记录总数。 |
| 最近试卷 | 便捷进入试卷管理。 |
| 最近成绩 | 便捷进入成绩记录。 |

#### 学生工作台

| 展示项 | 说明 |
| --- | --- |
| 可答试卷数量 | 学生可查看的已发布试卷总数。 |
| 成绩记录数量 | 学生个人提交记录数量。 |
| 未读提醒数量 | 当前学生未读考试提醒数量。 |
| 可答试卷 | 展示近期可查看的试卷。 |
| 最近成绩 | 展示本人最近提交记录。 |
| 考试提醒 | 展示本人近期提醒。 |

### 9.3 账号管理

账号管理仅管理员可访问，用于维护管理员和教师账号。

#### 9.3.1 账号列表

列表字段：

| 字段 | 说明 |
| --- | --- |
| 用户名 | 登录账号。 |
| 姓名 | 用户真实姓名。 |
| 角色 | 管理员或教师。 |
| 状态 | 启用或禁用。 |
| 操作 | 编辑、删除。 |

#### 9.3.2 新增账号

管理员可新增管理员或教师账号。

可填写字段：

| 字段 | 说明 |
| --- | --- |
| 用户名 | 必填，系统内唯一。 |
| 密码 | 可为空；为空时默认 `123456`。 |
| 姓名 | 必填。 |
| 角色 | 管理员或教师。 |
| 状态 | 启用或禁用。 |

业务规则：

| 规则 | 说明 |
| --- | --- |
| 不允许创建学生账号 | 学生账号必须在学生管理中创建。 |
| 用户名唯一 | 与已有账号重复会失败。 |
| 密码加密 | 密码使用 BCrypt 哈希保存。 |

#### 9.3.3 编辑账号

管理员可修改用户名、姓名、角色、状态和密码。

业务规则：

| 规则 | 说明 |
| --- | --- |
| 密码可不填 | 编辑时密码为空表示不修改原密码。 |
| 不能禁用当前登录账号 | 防止管理员将自己禁用。 |
| 不能将账号改为学生角色 | 学生账号在学生管理中维护。 |
| 用户名不能重复 | 修改后的用户名仍需全局唯一。 |

#### 9.3.4 删除账号

管理员可删除非当前登录账号。

业务规则：

| 规则 | 说明 |
| --- | --- |
| 不能删除当前登录账号 | 防止误删自身。 |
| 学生账号不在此模块维护 | 学生账号删除应通过学生管理完成。 |

### 9.4 学生管理

学生管理面向管理员和教师开放；教师只能查看，管理员可以增删改。

#### 9.4.1 学生列表与搜索

支持分页展示学生档案。

列表字段：

| 字段 | 说明 |
| --- | --- |
| 学号 | 学生唯一编号。 |
| 姓名 | 学生真实姓名。 |
| 用户名 | 学生登录账号。 |
| 班级 | 学生所在班级。 |
| 年级 | 学生年级。 |
| 学院 | 学生学院。 |
| 状态 | 账号启用或禁用。 |

搜索支持以下字段模糊匹配：

| 搜索字段 | 说明 |
| --- | --- |
| 学号 | 按学生编号搜索。 |
| 姓名 | 按学生姓名搜索。 |
| 班级 | 按班级搜索。 |
| 年级 | 按年级搜索。 |
| 学院 | 按学院搜索。 |

#### 9.4.2 新增学生

管理员可新增学生档案，同时系统会创建对应学生登录账号。

可填写字段：

| 字段 | 说明 |
| --- | --- |
| 用户名 | 必填，系统内唯一。 |
| 密码 | 可为空；为空时默认 `123456`。 |
| 姓名 | 必填。 |
| 学号 | 可为空；为空时系统自动生成。 |
| 班级 | 必填。 |
| 年级 | 必填。 |
| 学院 | 必填。 |
| 状态 | 启用或禁用。 |

学号规则：

| 规则 | 说明 |
| --- | --- |
| 手动填写 | 必须为 10 位数字。 |
| 自动生成 | 默认前缀为 `205244`，后 4 位基于当前最大学号递增。 |
| 初始示例 | 首个默认学生学号为 `2052443001`。 |
| 唯一性 | 学号不能与已有学生重复。 |

#### 9.4.3 编辑学生

管理员可修改学生账号和档案信息。

业务规则：

| 规则 | 说明 |
| --- | --- |
| 密码可不填 | 编辑时密码为空表示不修改。 |
| 学号为空时保留原学号 | 编辑场景下未传新学号则沿用已有学号。 |
| 学号格式校验 | 手动输入时必须为 10 位数字。 |
| 用户名和学号唯一 | 修改后不能与其他用户或学生重复。 |

#### 9.4.4 删除学生

管理员删除学生时，系统会删除学生档案以及对应用户账号。

### 9.5 试卷管理

试卷管理由管理员和教师使用，包含试卷基本信息维护和题目维护。

#### 9.5.1 试卷列表

列表字段：

| 字段 | 说明 |
| --- | --- |
| 试卷标题 | 试卷名称。 |
| 说明 | 试卷描述。 |
| 开始时间 | 学生可开始答题的时间。 |
| 结束时间 | 学生答题截止时间。 |
| 总分 | 当前试卷全部题目分值之和。 |
| 发布状态 | 已发布或未发布。 |
| 创建人 | 创建试卷的管理员或教师姓名。 |

管理员和教师可查看全部试卷；学生只能在在线答题入口查看已发布试卷。

#### 9.5.2 新增与编辑试卷

可维护字段：

| 字段 | 说明 |
| --- | --- |
| 标题 | 必填。 |
| 说明 | 可选。 |
| 开始时间 | 必填。 |
| 结束时间 | 必填。 |
| 发布状态 | 发布或暂不发布。 |

业务规则：

| 规则 | 说明 |
| --- | --- |
| 时间窗口校验 | 开始时间必须早于结束时间。 |
| 创建人记录 | 新增试卷时保存当前登录用户为创建人。 |
| 总分维护 | 试卷总分由题目分值自动汇总。 |

#### 9.5.3 删除试卷

管理员和教师可删除试卷。删除试卷时，系统会先删除该试卷下的题目，再删除试卷本身。

### 9.6 题目管理

题目管理在试卷管理弹窗中完成。

#### 9.6.1 支持题型

| 题型编码 | 题型名称 | 答题方式 | 判分方式 |
| --- | --- | --- | --- |
| `SINGLE_CHOICE` | 单选题 | 单选按钮 | 自动判分。 |
| `MULTIPLE_CHOICE` | 多选题 | 多选框 | 自动按集合判分。 |
| `TRUE_FALSE` | 判断题 | 单选按钮 | 自动判分。 |
| `FILL_BLANK` | 填空题 | 文本输入 | 自动判分。 |
| `SHORT_ANSWER` | 简答题 | 多行文本 | 默认待人工批改。 |

#### 9.6.2 题目字段

| 字段 | 说明 |
| --- | --- |
| 题干 | 必填。 |
| 类型 | 必填，选择五种题型之一。 |
| 选项 | 单选、多选、判断题可填写，每行一个选项。 |
| 标准答案 | 必填；多选题示例为 `A,C`。 |
| 分值 | 必填，必须大于 0。 |
| 排序 | 可选；为空时系统自动追加到最后。 |

#### 9.6.3 题目排序

题目按 `orderNo` 升序展示。新增题目不填写排序时，系统会取当前试卷最大排序号加 1；如果没有题目，则排序号为 1。

#### 9.6.4 总分重算

新增、编辑、删除题目后，系统会重新计算试卷总分，值为该试卷所有题目分值之和。

### 9.7 在线答题

在线答题仅学生可访问。

#### 9.7.1 试卷列表

学生只能看到已发布试卷，列表展示：

| 字段 | 说明 |
| --- | --- |
| 试卷 | 标题与说明。 |
| 开始时间 | 答题开始时间。 |
| 结束时间 | 答题截止时间。 |
| 总分 | 试卷总分。 |
| 状态 | 未开始、可答题、已结束。 |
| 操作 | 状态为可答题时显示开始答题。 |

#### 9.7.2 答题状态判断

| 状态 | 判断逻辑 | 是否可答 |
| --- | --- | --- |
| 未开始 | 当前时间早于开始时间。 | 否 |
| 可答题 | 当前时间在开始和结束时间之间。 | 是 |
| 已结束 | 当前时间晚于结束时间。 | 否 |

#### 9.7.3 答题页面

系统按题目排序展示题目。不同题型使用不同输入方式：

| 题型 | 输入方式 |
| --- | --- |
| 单选题 | 单选按钮。 |
| 多选题 | 多选框。 |
| 判断题 | 单选按钮。 |
| 填空题 | 单行文本。 |
| 简答题 | 多行文本。 |

提交前前端会弹出确认提示。提交成功后显示本次得分、总分、提交时间和每道题的判定结果。

#### 9.7.4 提交规则

后端提交时执行以下校验：

| 规则 | 说明 |
| --- | --- |
| 仅学生可提交 | 非学生角色不能提交答案。 |
| 试卷必须存在 | 不存在的试卷不能提交。 |
| 试卷必须已发布 | 未发布试卷不能提交。 |
| 当前时间必须在考试时间内 | 未开始或已结束都不能提交。 |
| 试卷必须有题目 | 没有题目的试卷不能提交。 |

当前实现允许学生对同一试卷多次提交，每次提交都会生成一条新的成绩记录。

### 9.8 自动判分与人工批改

#### 9.8.1 自动判分规则

| 题型 | 判分规则 |
| --- | --- |
| 单选题 | 学生答案与标准答案去除首尾空格、合并空白并转大写后比较。 |
| 判断题 | 同单选题逻辑。 |
| 填空题 | 同单选题逻辑。 |
| 多选题 | 将答案按逗号、中文逗号、分号、中文分号或空白拆分，去除尾部标点并转大写后按集合比较。 |
| 简答题 | 默认判为错误、得 0 分，显示为待批改。 |

#### 9.8.2 答题记录字段

每次提交会生成提交记录和每题答题记录。

提交记录包含：

| 字段 | 说明 |
| --- | --- |
| 试卷 | 所属试卷。 |
| 学生 | 提交学生。 |
| 得分 | 本次提交总得分。 |
| 总分 | 试卷总分。 |
| 提交时间 | 提交时间。 |

答题记录包含：

| 字段 | 说明 |
| --- | --- |
| 题目 | 对应题目。 |
| 学生答案 | 学生提交内容。 |
| 标准答案 | 提交时保存的标准答案。 |
| 是否正确 | 自动或人工判定结果。 |
| 本题得分 | 当前题目得分。 |

#### 9.8.3 人工批改

管理员和教师可在成绩详情中人工调整每道题的得分和判定结果。

业务规则：

| 规则 | 说明 |
| --- | --- |
| 分数范围 | 本题得分必须在 0 到题目满分之间。 |
| 判定结果 | 可手动选择正确或错误。 |
| 自动判定 | 如果未明确传入判定结果，则满分视为正确，否则视为错误。 |
| 总分同步 | 修改某题得分后，提交记录总得分会重新汇总。 |

### 9.9 成绩记录与导出

#### 9.9.1 成绩列表

管理员和教师可查看全部成绩记录，并可按试卷筛选；学生只能查看自己的成绩记录。

列表字段：

| 字段 | 说明 |
| --- | --- |
| 试卷 | 试卷名称。 |
| 学生 | 学生姓名。 |
| 学号 | 学生学号。 |
| 班级 | 学生班级。 |
| 学院 | 学生学院。 |
| 得分 | 本次提交得分和总分。 |
| 提交时间 | 答案提交时间。 |
| 操作 | 查看详情。 |

#### 9.9.2 成绩详情

详情展示每道题的题干、学生答案、标准答案、判定状态、题目满分和当前得分。管理员和教师可以在详情中保存新分数。

学生查看详情时，只能查看自己的提交记录，不能查看其他学生成绩。

#### 9.9.3 成绩导出

管理员和教师可导出 Excel 成绩表。支持导出全部提交记录，也支持按指定试卷导出。

导出文件名：

```text
成绩表.xlsx
```

导出列：

| 列名 | 说明 |
| --- | --- |
| 序号 | 导出行号。 |
| 试卷 | 试卷标题。 |
| 学号 | 学生学号。 |
| 姓名 | 学生姓名。 |
| 班级 | 学生班级。 |
| 年级 | 学生年级。 |
| 学院 | 学生学院。 |
| 得分 | 提交得分。 |
| 总分 | 试卷总分。 |
| 提交时间 | 提交时间。 |

### 9.10 考试提醒

考试提醒面向学生展示，由系统定时生成，也可由管理员或教师手动触发生成。

#### 9.10.1 自动生成规则

系统每天 08:00 执行提醒生成任务。

生成逻辑：

1. 读取配置 `app.reminder.days-before`，默认值为 3。
2. 计算目标日期为当前日期加 3 天。
3. 查询目标日期当天开始的已发布试卷。
4. 为每个学生生成对应试卷提醒。
5. 同一学生同一试卷只生成一条提醒。

提醒内容格式示例：

```text
试卷《数据结构预告测试》将在 2026-06-10 09:00 开始，请提前准备。
```

#### 9.10.2 手动生成提醒

管理员和教师可通过接口手动执行同样的提醒生成逻辑。

#### 9.10.3 学生查看提醒

学生在考试提醒页面可以查看：

| 字段 | 说明 |
| --- | --- |
| 试卷 | 关联试卷标题。 |
| 提醒内容 | 系统生成的提醒消息。 |
| 提醒时间 | 提醒生成时间。 |
| 状态 | 已读或未读。 |
| 操作 | 未读提醒可标记已读。 |

学生只能查看和操作自己的提醒。

### 9.11 资料文件

资料文件模块面向管理员、教师、学生开放，用于共享学习资料、试卷材料或图片资源。

#### 9.11.1 文件列表

列表字段：

| 字段 | 说明 |
| --- | --- |
| 文件名 | 原始文件名。 |
| 类型 | 文件 MIME 类型。 |
| 大小 | 文件大小，前端格式化为 B、KB 或 MB。 |
| 上传人 | 上传用户姓名。 |
| 上传时间 | 文件上传时间。 |
| 操作 | 下载、删除。 |

#### 9.11.2 上传文件

用户选择文件后，前端以 `multipart/form-data` 上传。后端会：

1. 校验文件不能为空。
2. 清理原始文件名并拒绝包含 `..` 的非法文件名。
3. 保留原始扩展名。
4. 生成 UUID 作为存储文件名。
5. 保存到上传目录。
6. 记录原始文件名、存储文件名、类型、大小、路径、上传人和时间。

上传大小限制由 Spring Boot multipart 配置控制，当前最大为 50MB。

#### 9.11.3 下载文件

文件下载接口为公开 GET 接口，下载地址格式为：

```text
/api/files/{id}/download
```

前端通过该地址发起下载。

#### 9.11.4 删除文件

删除规则：

| 用户 | 删除权限 |
| --- | --- |
| 管理员 | 可删除任意文件。 |
| 教师 | 只能删除自己上传的文件。 |
| 学生 | 只能删除自己上传的文件。 |

删除时系统会同时删除数据库记录和磁盘文件。

### 9.12 讨论区

讨论区面向所有登录用户开放，支持围绕试卷进行交流。

#### 9.12.1 讨论列表

支持分页展示讨论，并按更新时间倒序排列。

支持筛选：

| 筛选项 | 说明 |
| --- | --- |
| 关键词 | 按标题或内容模糊搜索。 |
| 关联试卷 | 只查看某个试卷相关讨论。 |

列表展示：

| 字段 | 说明 |
| --- | --- |
| 标题 | 讨论标题。 |
| 内容摘要 | 讨论内容。 |
| 关联试卷 | 可为空。 |
| 作者 | 作者姓名和角色。 |
| 回复数 | 当前回复数量。 |
| 更新时间 | 最后更新时间。 |

#### 9.12.2 发起讨论

用户可填写标题、内容，并可选择关联试卷。关联试卷为空时表示普通讨论。

业务规则：

| 规则 | 说明 |
| --- | --- |
| 标题必填 | 不能为空。 |
| 内容必填 | 不能为空。 |
| 关联试卷必须存在 | 如果选择试卷 ID，后端会校验试卷存在。 |
| 作者记录 | 系统记录当前登录用户为作者。 |

#### 9.12.3 回复讨论

用户可在讨论详情中回复。回复成功后会更新讨论的更新时间。

#### 9.12.4 删除讨论和回复

删除规则：

| 用户 | 删除权限 |
| --- | --- |
| 管理员 | 可删除任意讨论和回复。 |
| 教师 | 只能删除自己发布的讨论和回复。 |
| 学生 | 只能删除自己发布的讨论和回复。 |

删除讨论时，系统会先删除该讨论下的回复，再删除讨论本身。

### 9.13 个人中心

个人中心面向所有登录用户开放。

#### 9.13.1 个人资料展示

展示当前用户：

| 字段 | 说明 |
| --- | --- |
| 用户名 | 登录账号。 |
| 姓名 | 用户真实姓名。 |
| 角色 | 管理员、教师或学生。 |
| 头像 | 当前头像地址或默认首字头像。 |

学生用户额外展示：

| 字段 | 说明 |
| --- | --- |
| 学号 | 学生学号。 |
| 班级 | 学生班级。 |
| 年级 | 学生年级。 |
| 学院 | 学生学院。 |

#### 9.13.2 修改资料

用户可修改姓名和头像地址。

头像支持三种方式：

| 方式 | 说明 |
| --- | --- |
| 粘贴图片地址 | 手动输入头像 URL。 |
| 选择预设头像 | 前端内置多个 SVG data URL 头像。 |
| 上传图片 | 调用资料文件上传接口，上传成功后使用下载地址作为头像。 |

保存成功后，系统会刷新 Redis 中的当前登录态，使页面顶部用户信息同步更新。

#### 9.13.3 修改密码

用户输入原密码、新密码和确认密码后修改密码。

业务规则：

| 规则 | 说明 |
| --- | --- |
| 原密码必须正确 | 后端使用 BCrypt 校验原密码。 |
| 新密码至少 6 位 | 后端强制校验。 |
| 两次输入一致 | 前端校验新密码与确认密码。 |
| 新密码加密存储 | 修改后的密码使用 BCrypt 哈希保存。 |

## 10. 通用交互与分页

### 10.1 分页

系统多个列表页面共用分页组件，包括账号、学生、试卷、成绩、文件、讨论等。

分页返回字段：

| 字段 | 说明 |
| --- | --- |
| `records` | 当前页数据列表。 |
| `total` | 总记录数。 |
| `page` | 当前页码，从 1 开始。 |
| `size` | 每页数量。 |
| `pages` | 总页数。 |

后端分页规则：

| 规则 | 说明 |
| --- | --- |
| 页码最小值 | 小于 1 时按 1 处理。 |
| 每页数量最小值 | 小于 1 时按 1 处理。 |
| 每页数量最大值 | 最大限制为 100。 |

前端支持每页 10、20、50 条切换。

### 10.2 统一响应格式

普通 JSON 接口统一返回：

```json
{
  "success": true,
  "message": "操作成功",
  "data": {}
}
```

失败时：

```json
{
  "success": false,
  "message": "错误信息",
  "data": null
}
```

前端 Axios 拦截器会自动解包成功响应中的 `data` 字段；当响应失败或返回 401 时，会清理本地登录态并跳转登录页。

### 10.3 异常处理

| 异常类型 | HTTP 状态 | 返回说明 |
| --- | --- | --- |
| 业务异常 | 400 | 返回业务错误消息。 |
| 参数校验异常 | 400 | 返回参数不合法消息。 |
| 权限不足 | 403 | 返回没有权限执行该操作。 |
| 未登录 | 401 | 返回请先登录。 |
| 其他异常 | 500 | 返回系统异常消息。 |

## 11. 主要业务流程

### 11.1 教师出题到学生答题流程

1. 教师或管理员登录系统。
2. 进入试卷管理，创建试卷并设置开始时间、结束时间和发布状态。
3. 在题目管理中添加题目，设置题型、选项、标准答案、分值和排序。
4. 系统自动汇总试卷总分。
5. 学生登录系统后进入在线答题。
6. 学生只能看到已发布试卷。
7. 到达答题时间后，学生进入答题页面填写答案。
8. 学生提交答案。
9. 系统校验试卷状态和时间窗口。
10. 系统生成提交记录和每题答题记录。
11. 客观题自动判分，简答题默认待批改。
12. 学生可在成绩记录中查看本人成绩。

### 11.2 简答题批改流程

1. 管理员或教师进入成绩记录。
2. 可按试卷筛选提交记录。
3. 打开某条成绩详情。
4. 查看每道题的学生答案、标准答案、当前得分和判定状态。
5. 对简答题或其他需要调整的题目输入得分并选择判定结果。
6. 保存后系统更新该题得分。
7. 系统重新汇总该次提交总得分。

### 11.3 成绩导出流程

1. 管理员或教师进入成绩记录。
2. 选择全部试卷或某一试卷。
3. 点击导出成绩表。
4. 后端查询对应提交记录。
5. 使用 Apache POI 生成 Excel 文件。
6. 浏览器下载 `成绩表.xlsx`。

### 11.4 学生账号创建流程

1. 管理员进入学生管理。
2. 点击新增学生。
3. 填写用户名、姓名、班级、年级、学院等信息。
4. 可手动填写 10 位数字学号，也可留空自动生成。
5. 密码为空时默认设置为 `123456`。
6. 系统创建用户账号，角色固定为学生。
7. 系统创建学生档案并绑定账号。

### 11.5 考试提醒流程

1. 系统每天 08:00 执行提醒任务，或由管理员/教师手动触发。
2. 系统查找 3 天后开始的已发布试卷。
3. 系统为每个学生创建提醒。
4. 同一试卷和学生只创建一次提醒。
5. 学生进入考试提醒页面查看提醒。
6. 学生可将未读提醒标记为已读。

### 11.6 文件共享流程

1. 登录用户进入资料文件。
2. 点击上传文件并选择本地文件。
3. 系统保存文件到上传目录并记录元数据。
4. 所有用户可在列表中查看并下载文件。
5. 管理员可删除任意文件，其他用户只能删除自己上传的文件。

### 11.7 讨论交流流程

1. 登录用户进入讨论区。
2. 可按关键词或试卷筛选讨论。
3. 用户发起讨论，可选择关联试卷。
4. 其他用户查看讨论详情并回复。
5. 回复后讨论更新时间更新，列表排序随之变化。
6. 管理员可删除任意内容，普通用户只能删除自己发布的内容。

## 12. 接口清单

### 12.1 认证接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `POST` | `/api/auth/login` | 公开 | 用户登录。 |
| `POST` | `/api/auth/register` | 公开 | 教师注册并登录。 |
| `POST` | `/api/auth/logout` | 登录用户 | 退出登录并删除 Redis token。 |
| `GET` | `/api/auth/me` | 登录用户 | 获取当前登录用户信息。 |

### 12.2 个人中心接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `GET` | `/api/profile/me` | 登录用户 | 获取个人资料。 |
| `PUT` | `/api/profile/me` | 登录用户 | 修改姓名和头像，并刷新登录态。 |
| `PUT` | `/api/profile/password` | 登录用户 | 修改密码。 |

### 12.3 账号管理接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `GET` | `/api/users?page=1&size=10` | 管理员 | 分页查询账号。 |
| `POST` | `/api/users` | 管理员 | 新增管理员或教师账号。 |
| `PUT` | `/api/users/{id}` | 管理员 | 编辑账号。 |
| `DELETE` | `/api/users/{id}` | 管理员 | 删除账号。 |

### 12.4 学生管理接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `GET` | `/api/students?keyword=&page=1&size=10` | 管理员、教师 | 分页查询学生，支持关键词搜索。 |
| `GET` | `/api/students/{id}` | 管理员、教师 | 查看学生详情。 |
| `POST` | `/api/students` | 管理员 | 新增学生账号和档案。 |
| `PUT` | `/api/students/{id}` | 管理员 | 编辑学生账号和档案。 |
| `DELETE` | `/api/students/{id}` | 管理员 | 删除学生账号和档案。 |

### 12.5 试卷与题目接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `GET` | `/api/papers?page=1&size=10` | 登录用户 | 查询试卷；学生只返回已发布试卷。 |
| `GET` | `/api/papers/{id}` | 登录用户 | 查看试卷详情。 |
| `POST` | `/api/papers` | 管理员、教师 | 新增试卷。 |
| `PUT` | `/api/papers/{id}` | 管理员、教师 | 编辑试卷。 |
| `DELETE` | `/api/papers/{id}` | 管理员、教师 | 删除试卷。 |
| `GET` | `/api/papers/{paperId}/questions` | 登录用户 | 查询题目；学生不返回标准答案，管理员/教师返回标准答案。 |
| `POST` | `/api/papers/{paperId}/questions` | 管理员、教师 | 新增题目。 |
| `PUT` | `/api/papers/{paperId}/questions/{questionId}` | 管理员、教师 | 编辑题目。 |
| `DELETE` | `/api/papers/{paperId}/questions/{questionId}` | 管理员、教师 | 删除题目。 |

### 12.6 答题与成绩接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `POST` | `/api/submissions/papers/{paperId}` | 学生 | 提交试卷答案。 |
| `GET` | `/api/submissions?paperId=&page=1&size=10` | 登录用户 | 查询成绩记录；学生只查本人记录。 |
| `GET` | `/api/submissions/{id}` | 登录用户 | 查看成绩详情；学生只能查看本人记录。 |
| `PUT` | `/api/submissions/{submissionId}/answers/{answerId}/score` | 管理员、教师 | 人工修改某题得分和判定结果。 |
| `GET` | `/api/submissions/export?paperId=` | 管理员、教师 | 导出成绩 Excel。 |

### 12.7 考试提醒接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `GET` | `/api/reminders/my` | 学生 | 查询本人考试提醒。 |
| `PUT` | `/api/reminders/{id}/read` | 学生 | 标记本人提醒为已读。 |
| `POST` | `/api/reminders/generate` | 管理员、教师 | 手动生成考前提醒。 |

### 12.8 文件接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `GET` | `/api/files?page=1&size=10` | 登录用户 | 分页查询文件。 |
| `POST` | `/api/files` | 登录用户 | 上传文件。 |
| `GET` | `/api/files/{id}/download` | 公开 | 下载文件。 |
| `DELETE` | `/api/files/{id}` | 登录用户 | 删除文件；管理员可删任意文件，其他用户只能删自己的文件。 |

### 12.9 讨论接口

| 方法 | 路径 | 权限 | 功能 |
| --- | --- | --- | --- |
| `GET` | `/api/discussions?keyword=&paperId=&page=1&size=10` | 登录用户 | 分页查询讨论，支持关键词和试卷筛选。 |
| `GET` | `/api/discussions/{id}` | 登录用户 | 查看讨论详情和回复。 |
| `POST` | `/api/discussions` | 登录用户 | 发起讨论。 |
| `POST` | `/api/discussions/{id}/replies` | 登录用户 | 回复讨论。 |
| `DELETE` | `/api/discussions/{id}` | 登录用户 | 删除讨论；管理员或作者可删。 |
| `DELETE` | `/api/discussions/{postId}/replies/{replyId}` | 登录用户 | 删除回复；管理员或作者可删。 |

## 13. 数据库表说明

### 13.1 用户表 `users`

保存系统登录账号。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `username` | 用户名，唯一。 |
| `password_hash` | BCrypt 密码哈希。 |
| `real_name` | 真实姓名。 |
| `role` | 角色：`ADMIN`、`TEACHER`、`STUDENT`。 |
| `enabled` | 是否启用。 |
| `avatar_url` | 头像地址。 |
| `created_at` | 创建时间。 |
| `updated_at` | 更新时间。 |

### 13.2 学生档案表 `student_profiles`

保存学生扩展信息，并与用户账号一对一绑定。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `user_id` | 关联用户 ID，唯一。 |
| `student_no` | 学号，唯一。 |
| `class_name` | 班级。 |
| `grade` | 年级。 |
| `college` | 学院。 |
| `created_at` | 创建时间。 |
| `updated_at` | 更新时间。 |

### 13.3 试卷表 `papers`

保存试卷基础信息。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `title` | 试卷标题。 |
| `description` | 试卷说明。 |
| `start_time` | 开始时间。 |
| `end_time` | 结束时间。 |
| `total_score` | 试卷总分。 |
| `published` | 是否发布。 |
| `created_by_id` | 创建人用户 ID。 |
| `created_at` | 创建时间。 |
| `updated_at` | 更新时间。 |

### 13.4 题目表 `questions`

保存试卷题目。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `paper_id` | 所属试卷 ID。 |
| `title` | 题干。 |
| `type` | 题型。 |
| `options_text` | 选项文本。 |
| `correct_answer` | 标准答案。 |
| `score` | 题目分值。 |
| `order_no` | 排序号。 |
| `created_at` | 创建时间。 |
| `updated_at` | 更新时间。 |

### 13.5 提交记录表 `submissions`

保存学生每次提交试卷的汇总信息。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `paper_id` | 试卷 ID。 |
| `student_id` | 学生档案 ID。 |
| `score` | 本次得分。 |
| `total_score` | 试卷总分。 |
| `submitted_at` | 提交时间。 |

### 13.6 学生答题表 `student_answers`

保存每次提交中每道题的答题详情。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `submission_id` | 提交记录 ID。 |
| `question_id` | 题目 ID。 |
| `answer_text` | 学生答案。 |
| `correct_answer` | 标准答案快照。 |
| `correct_flag` | 是否正确。 |
| `score` | 本题得分。 |

### 13.7 考试提醒表 `exam_reminders`

保存学生考前提醒。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `paper_id` | 试卷 ID。 |
| `student_id` | 学生档案 ID。 |
| `message` | 提醒内容。 |
| `remind_at` | 提醒时间。 |
| `read_flag` | 是否已读。 |
| `created_at` | 创建时间。 |

约束：同一试卷和同一学生只能有一条提醒。

### 13.8 上传文件表 `uploaded_files`

保存上传文件元数据。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `original_name` | 原始文件名。 |
| `stored_name` | 存储文件名。 |
| `content_type` | 文件 MIME 类型。 |
| `file_size` | 文件大小。 |
| `storage_path` | 磁盘保存路径。 |
| `uploader_id` | 上传人 ID。 |
| `created_at` | 上传时间。 |

### 13.9 讨论主帖表 `discussion_posts`

保存讨论主帖。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `title` | 标题。 |
| `content` | 内容。 |
| `paper_id` | 关联试卷 ID，可为空。 |
| `author_id` | 作者用户 ID。 |
| `created_at` | 创建时间。 |
| `updated_at` | 更新时间。 |

### 13.10 讨论回复表 `discussion_replies`

保存讨论回复。

| 字段 | 说明 |
| --- | --- |
| `id` | 主键。 |
| `post_id` | 主帖 ID。 |
| `content` | 回复内容。 |
| `author_id` | 回复人用户 ID。 |
| `created_at` | 回复时间。 |

## 14. 安全与权限机制

### 14.1 Token 登录态

系统采用无状态 HTTP 会话。登录成功后生成 token，Redis 保存 token 对应的用户信息。

Redis key 格式：

```text
auth:token:{token}
```

token 默认有效期为 12 小时。

### 14.2 请求认证

前端请求时携带：

```text
Authorization: Bearer <token>
```

后端过滤器读取 token 后从 Redis 查询用户信息，并写入 Spring Security 上下文。

### 14.3 密码安全

所有用户密码均使用 BCrypt 保存，不明文落库。管理员新增账号或学生时，未填写密码则默认设置为 `123456`。

### 14.4 接口授权

后端基于 Spring Security 方法级权限控制，例如：

| 权限表达式 | 说明 |
| --- | --- |
| `hasRole('ADMIN')` | 仅管理员。 |
| `hasRole('STUDENT')` | 仅学生。 |
| `hasAnyRole('ADMIN','TEACHER')` | 管理员或教师。 |

### 14.5 CORS 与下载

系统允许跨域请求，允许的 HTTP 方法包括 `GET`、`POST`、`PUT`、`DELETE`、`OPTIONS`。文件下载接口暴露 `Content-Disposition` 响应头，便于浏览器识别下载文件名。

## 15. 业务规则汇总

| 模块 | 规则 |
| --- | --- |
| 登录 | 账号必须存在、已启用且密码正确。 |
| 注册 | 公开注册仅支持教师账号。 |
| 学生账号 | 只能由管理员在学生管理中创建。 |
| 默认密码 | 管理员新增账号或学生时，密码为空则为 `123456`。 |
| 学号 | 必须为 10 位数字；为空时自动生成。 |
| 试卷 | 开始时间必须早于结束时间。 |
| 试卷可见性 | 学生只看到已发布试卷；管理员和教师看到全部试卷。 |
| 答题 | 学生只能在试卷已发布且当前时间位于开始和结束时间之间时提交。 |
| 提交次数 | 当前实现允许同一学生对同一试卷多次提交。 |
| 判分 | 客观题自动判分，简答题默认待人工批改。 |
| 改分 | 管理员和教师可改分，单题分数不能小于 0 或大于题目满分。 |
| 成绩查看 | 学生只能查看自己的成绩；管理员和教师可查看全部成绩。 |
| 成绩导出 | 仅管理员和教师可导出。 |
| 考试提醒 | 默认提前 3 天，每天 08:00 生成，同一学生同一试卷不重复生成。 |
| 文件删除 | 管理员可删除任意文件，其他用户只能删除自己上传的文件。 |
| 讨论删除 | 管理员可删除任意讨论和回复，其他用户只能删除自己发布的内容。 |
| 修改密码 | 原密码必须正确，新密码至少 6 位。 |

## 16. 当前实现边界与注意事项

| 项目 | 说明 |
| --- | --- |
| 同卷重复提交 | 系统未限制学生重复提交同一试卷，会保留多条提交记录。 |
| 简答题初始得分 | 简答题提交后默认为 0 分，需要管理员或教师人工批改。 |
| 文件下载权限 | 文件下载接口当前为公开访问，知道下载地址即可访问文件。 |
| 文件类型限制 | 普通资料上传未限制文件类型；个人头像上传在前端限制为图片。 |
| 学生资料修改 | 学生本人不能在个人中心修改学号、班级、年级、学院，只能由管理员在学生管理中维护。 |
| 账号管理范围 | 账号管理模块不维护学生账号，学生账号由学生管理模块维护。 |
| 定时提醒依赖 | 自动提醒依赖后端服务持续运行和系统时间正确。 |

## 17. 页面操作入口汇总

| 操作目标 | 推荐入口 |
| --- | --- |
| 登录系统 | `/login` |
| 注册教师账号 | `/register` |
| 查看统计与近期数据 | 工作台 |
| 新增管理员或教师 | 账号管理 |
| 新增学生账号 | 学生管理 |
| 创建试卷 | 试卷管理 |
| 维护题目 | 试卷管理中的题目按钮 |
| 学生答题 | 在线答题 |
| 查看成绩详情 | 成绩记录中的详情按钮 |
| 批改简答题 | 成绩详情中的保存分数 |
| 导出成绩表 | 成绩记录中的导出成绩表 |
| 上传资料 | 资料文件 |
| 发起讨论 | 讨论区 |
| 查看考试提醒 | 考试提醒 |
| 修改头像或密码 | 个人中心 |

## 18. 总结

学生在线答题系统已经覆盖在线考试场景中的核心功能：用户与角色管理、学生档案管理、试卷题库维护、学生在线答题、自动判分、人工批改、成绩查询导出、考前提醒、资料共享和讨论交流。系统采用前后端分离架构，后端通过 Redis token 和 Spring Security 控制登录态与权限，前端根据角色展示对应菜单和页面能力，适合作为学校课程测验、课堂练习和在线考试管理系统的基础版本。
